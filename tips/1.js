// if (!value) checks if value is falsy.
// Since NaN, 0, null, and "" are all falsy in JavaScript, the loop breaks in such cases.

// it can also be written as
// if (value !== NaN || value !== 0 || value !== ......)