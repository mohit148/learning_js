// alert, prompt, confirm, and console.log are built-in JavaScript functions you can use in a browser.

// alert, prompt, confirm, and console.log are built-in JavaScript functions you can use in a browser.

// alert: shows a message box.

// prompt: asks the user to type something.

// confirm: asks the user to choose OK or Cancel.

// console.log: prints messages to the browser’s console (used for debugging).



// Note:
// * console.log() and alert() are designed just to do something (output), not return anything useful — so they return undefined.
// * prompt() is designed to get something from the user — so it returns the input (as a string), or null if canceled.

// * confirm() returns a boolean value:
// true if the user clicks Ok
// false if the user clicks Cancel
