// For every loop iteration, write down which value it outputs and then compare it with the solution.

// Both loops alert the same values, or not?

// The prefix form ++i:

let i = 0;
while (++i < 5) alert( i );
// The postfix form i++

let j = 0;
while (j++ < 5) alert (j);



// for first, 1,2,3,4,
// for second, 1, 2, 3, 4, 5

// * the prefix form of i increment(++i) first increments i and then returns the new value
// * The postfix form of i increment(i++) first increments i and then returns the old value


